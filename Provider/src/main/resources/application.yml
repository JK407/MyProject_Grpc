server:
  port: 8010
spring:
  application:
    name: EurekaProvider
  profiles:
    active:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/grpc_goods
    username: root
    password: root
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7010/eureka/

